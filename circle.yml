machine:
  timezone: Europe/Berlin

test:
  override:
    - ./gradlew test -p library
  post:
    - ./gradlew cobertura coveralls -p library

deployment:
  staging:
    branch: development
    commands:
      - ./gradlew bintrayUpload -p library
#    production:
#        branch: master
#        commands:
#            - ./gradlew bintrayUpload


